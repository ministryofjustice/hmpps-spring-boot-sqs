server:
  shutdown: immediate

management.endpoint:
  health.cache.time-to-live: 0
  info.cache.time-to-live: 0

hmpps.sqs:
  provider: localstack
  queues:
    mainQueue:
      dlqName: ${random.uuid}
      queueName: ${random.uuid}
    anotherQueue:
      dlqName: ${random.uuid}
      queueName: ${random.uuid}
